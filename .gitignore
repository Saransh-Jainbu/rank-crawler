node_modules
.next
out
dist
.env
.env.local
.env.*.local
.DS_Store
*.pem
.git

# Data files (public/data.json is generated by setup.py/setup.bat)
# but we can check it in or not depending on preference
# public/data.json
